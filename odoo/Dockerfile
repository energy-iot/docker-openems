# Use Odoo 16.0 as the base image
FROM odoo:16.0

# Environment variables for database connection (examples; adjust as necessary)
ENV HOST=db
ENV PORT=5432
ENV USER=odoo
ENV PASSWORD=odoo
ENV DB_NAME=postgres

# Expose port 8069 for Odoo web interface
EXPOSE 8069